export type Product = {
  id: string;
  name: string;
  slug: string;
  brand: string;
  model: string;
  category: string;
  imageUrl: string;
  description: string;
  active: boolean;
  createdAt: string;
  updatedAt: string;
};
